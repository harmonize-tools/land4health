# Internal wrapper around rgee::rdate_to_eedate Mockable with with_mocked_bindings in tests.

Internal wrapper around rgee::rdate_to_eedate Mockable with
with_mocked_bindings in tests.

## Usage

``` r
l4h_to_eedate(x)
```
