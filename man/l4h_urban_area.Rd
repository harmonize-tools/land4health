% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/l4h_urban_area.R
\name{l4h_urban_area}
\alias{l4h_urban_area}
\title{Extract Urban Area from MODIS Land Cover (MCD12Q1.061)}
\usage{
l4h_urban_area(
  from,
  to,
  region,
  scale = 500,
  lc_band = "LC_Type2",
  sf = TRUE,
  quiet = FALSE,
  force = FALSE,
  ...
)
}
\arguments{
\item{from}{Character string indicating the starting year (e.g., \code{"2010"}).}

\item{to}{Character string indicating the ending year (e.g., \code{"2020"}).}

\item{region}{A \code{sf} object representing the region of interest.}

\item{scale}{Numeric. The nominal scale in meters to use for the area calculation (e.g., \code{500}).}

\item{lc_band}{Character. The MODIS land cover classification band to use. One of:
\code{"LC_Type1"} (default), \code{"LC_Type2"}, \code{"LC_Type3"}, \code{"LC_Type4"}, or \code{"LC_Type5"}.}

\item{sf}{Logical. Return result as an \code{sf} object? Default is \code{TRUE}.}

\item{quiet}{Logical. If \code{TRUE}, suppress the progress bar (default \code{FALSE}).}

\item{force}{Logical. Force request extract.}

\item{...}{arguments of \code{ee_extract} of \code{rgee} packages.}
}
\value{
A \code{tibble} containing the year and area (in km²) of the selected land cover class.
}
\description{
Extracts the annual area of a specific land cover class from the MODIS Land Cover product (MCD12Q1.061),
using the specified land cover classification scheme (band).

\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
This function uses the MODIS Land Cover Type Yearly Global 500m dataset (MCD12Q1.061) from Google Earth Engine.
It calculates the area of the land cover class \code{"13"} (Urban and Built-Up Lands) based on the selected band.
\subsection{Available Classification Schemes:}{
\itemize{
\item \strong{LC_Type1}: IGBP global vegetation classification (17 classes)
\item \strong{LC_Type2}: University of Maryland (UMD) land cover classification
\item \strong{LC_Type3}: MODIS LAI/FPAR Biome Type classification
\item \strong{LC_Type4}: MODIS Net Primary Production (NPP) Biome classification
\item \strong{LC_Type5}: Plant Functional Type (PFT) classification
}

\strong{Note:} The function currently extracts only class \code{13} (Urban), which may correspond to different meanings depending on the selected \code{lc_band}.

For detailed class definitions of each scheme, refer to:
\href{https://developers.google.com/earth-engine/datasets/catalog/MODIS_061_MCD12Q1}{MODIS/061/MCD12Q1 on Google Earth Engine}
}
}
\examples{
\dontrun{

library(land4health)
library(geoidep)
ee_Initialize()
region <- get_districts()
region_ee <- pol_as_ee(region, id = 'distr' ,simplify = 1000)
data <- get_urban(from = 2008, to = 2010, region = region)

}
}
