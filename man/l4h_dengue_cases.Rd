% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/l4h_dengue_cases.R
\name{l4h_dengue_cases}
\alias{l4h_dengue_cases}
\title{Extract dengue case data from OpenDengue}
\source{
Data from the \href{https://opendengue.org}{OpenDengue Project}.
}
\usage{
l4h_dengue_cases(
  from,
  to,
  data_type = c("temporal", "spatial", "national"),
  region = NULL,
  country = "Peru",
  cache = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{from}{Start date (\code{YYYY-MM-DD}).}

\item{to}{End date (\code{YYYY-MM-DD}).}

\item{data_type}{One of \code{"national"}, \code{"spatial"}, or \code{"temporal"}.}

\item{region}{Region code or full name (case-insensitive). Codes: \code{"paho"}, \code{"searo"},
\code{"wpro"}, \code{"afro"}, \code{"emro"}, \code{"euro"}. Full names: "Pan-American Region",
"South-East Asia Region", "Western Pacific Region", "African Region",
"Eastern Mediterranean Region", "European Region".}

\item{country}{Country name (case-insensitive, e.g., \code{"peru"}), matched against \code{adm_0_name}.}

\item{cache}{Logical. If \code{TRUE}, caches the downloaded ZIP locally.}

\item{quiet}{Logical. If \code{TRUE}, prints progress status via \strong{cli}.}
}
\value{
A tibble with columns: \code{date_start}, \code{date_end}, \code{cases}, \code{state}, \code{area}, plus other fields.
}
\description{
A global database of publicly available dengue case data.
\strong{The OpenDengue Project} provides a harmonized, open-access repository of dengue surveillance data
from national ministries of health across Latin America and other regions. This function enables
programmatic access to weekly case counts by downloading, caching, unzipping, reading, and filtering
national, spatial, or temporal extracts by region and country for a specified date range, returning
a ready-to-use tibble.

\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}
}
\section{Credits}{

\if{html}{\figure{innovalab-badges.svg}}
\itemize{
\item Developed by the Innovalab Team \href{mailto:info@innovalab.com}{info@innovalab.com}
\item Innovalab: Pioneering geospatial health analytics and open‐science tools.
\item Stay connected: follow us on Twitter \href{https://twitter.com/InnovalabGeo}{@InnovalabGeo}
}
}

\examples{
if (interactive()) {
  # National extract for Peru in 2019
  df_nat <- l4h_dengue_cases(
    from = "2019-01-01",
    to = "2019-12-31",
    data_type = "national",
    region = "paho",
    country = "peru",
    cache = TRUE,
    quiet = TRUE
  )
  head(df_nat)

  # Spatial extract for Brazil
  df_spat <- l4h_dengue_cases(
    from = "2021-01-01",
    to = "2021-12-31",
    data_type = "spatial",
    region = "Pan-American Region",
    country = "brazil",
    cache = TRUE,
    quiet = TRUE
  )
  head(df_spat)

  # Temporal extract for Argentina
  df_temp <- l4h_dengue_cases(
    from = "2020-01-01",
    to = "2020-12-31",
    data_type = "temporal",
    region = "PAHO",
    country = "Argentina",
    cache = TRUE,
    quiet = TRUE
  )
  head(df_temp)
}
}
\references{
Morales, I. et al. (2024). OpenDengue: Harmonized dengue surveillance data for Latin America.
}
