% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_forest_loss.R
\name{calculate_forest_loss}
\alias{calculate_forest_loss}
\title{Calculate Forest Loss}
\usage{
calculate_forest_loss(
  from,
  to,
  region,
  fun = "mean",
  progress = FALSE,
  sf = TRUE
)
}
\arguments{
\item{from}{A numeric year between 2001 and 2023. Indicates the start of the analysis period.}

\item{to}{A numeric year between 2001 and 2023. Indicates the end of the analysis period.}

\item{region}{A \code{sf} object representing the area of interest.}

\item{fun}{A string indicating the reducer to apply (e.g., \code{"mean"}, \code{"sum"}). Default is \code{"mean"}.}

\item{progress}{Logical. Show progress bar? Default is \code{FALSE}.}

\item{sf}{Logical. Return result as an \code{sf} object? Default is \code{TRUE}.}
}
\value{
A \code{data.frame} or \code{sf} object with forest loss per year in square kilometers.
}
\description{
Calculates forest loss within a user-defined region for a specified year range.
}
\details{
Forest loss is derived from the Hansen Global Forest Change dataset (\code{UMD/hansen/global_forest_change_2023_v1_11}).
The \code{lossyear} band encodes the year of forest cover loss as follows:
\itemize{
\item Values range from \strong{1 to 23}, corresponding to the years \strong{2001 to 2023}.
\item A value of \strong{0} indicates \strong{no forest loss} detected.
\item Forest loss is defined as a \strong{stand-replacement disturbance}, or a change from forest to non-forest state.
}

This function uses those values to calculate annual forest loss within the specified region and year range.
}
\section{Lifecycle}{

\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}
}

\examples{
\dontrun{
library(rgee)
library(sf)
ee_Initialize()

# Define region as a bounding box polygon
region <- st_as_sf(st_sfc(
  st_polygon(list(matrix(c(
    -74.1, -4.4,
    -74.1, -3.7,
    -73.2, -3.7,
    -73.2, -4.4,
    -74.1, -4.4
  ), ncol = 2, byrow = TRUE))),
  crs = 4326
))

# Run forest loss calculation
result <- calculate_forest_loss(from = 2005, to = 2007, region = region)
print(result)
}

}
\references{
Hansen, M. C., Potapov, P. V., Moore, R., Hancher, M., Turubanova, S. A., Tyukavina, A., ... & Townshend, J. R. G. (2013).
\emph{High-Resolution Global Maps of 21st-Century Forest Cover Change}. Science, 342(6160), 850–853.
DOI: \doi{10.1126/science.1244693}
}
